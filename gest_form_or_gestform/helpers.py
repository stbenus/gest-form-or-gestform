def is_number_divisible_by_divisor(dividend, divisor):
    if dividend % divisor == 0:
        return True
    return False

def is_number_divisible_by_divisors(dividend, divisors):
    # A while that call is_number_divisible_by_divisor
    return None